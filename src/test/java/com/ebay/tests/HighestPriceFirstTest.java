package com.ebay.tests;

/*import org.testng.annotations.Test;

import com.ebay.utils.BrowserUtil;
import com.ebay.webpages.HomePage;
import com.ebay.webpages.ResultsPage;

public class HighestPriceFirstTest{
	
	@Test(dataProvider="highestPriceFirst",dataProviderClass=TestData.class)
	public void verifyHighestPriceFirst(String url, String product,String expString)
	{
		HomePage homePage=new HomePage(driver);
		homePage.loadUrl(url);
		homePage.searchProduct(product);
		ResultsPage resultPage=new ResultsPage(driver);
		resultPage.verifyBestMatch(expString);
		resultPage.selectHighestPriceFirst();
	}

}*/
